# Feature: 智能文档版本管理与同步更新机制

## Context
基于现有的“占位符填充”功能，我们需要实现一套机制，以平衡“数据自动同步”与“用户手动微调”的需求。

## Core Logic (状态机驱动)

### 1. 文件状态定义
在采购项目下区域 `流程文件` 列表中增加字段列 `sync_status`:
- `SYNCED` (默认): 文件内容与数据库完全一致。
- `USER_MODIFIED`: 用户手动修改了非占位符内容（通过检测文件最后修改时间晚于生成时间，或解析文档元数据标记）。
- `OUTDATED`: 关联的采购项目数据发生变更，文件内容已过时，需更新。

### 2. 用户手动修改检测
- **前端**: 当用户在浏览器中点击“文件名”进入本地文件编制后保存时，系统将 `sync_status` 更新为 `USER_MODIFIED`。
- **视觉标记**: 在文件列表中，该类文件显示蓝色图标/标签：“含手动修改”。
- **规范**: 要求用户在 Word 中手动修改的内容必须使用**红色字体**。

### 3. 数据变更触发更新 (关键流程)
当用户修改“四步式”表单内容并提交保存时：
1.  **后端扫描**: 查找该项目下所有 `template_type` 为标准流程文件的记录。
2.  **状态更新**:
    - 若文件状态为 `SYNCED`: 直接调用填充服务，用新数据**覆盖**原文件。状态保持 `SYNCED`。
    - 若文件状态为 `USER_MODIFIED`: 
      - **不要直接覆盖！**
      - **步骤 A (备份)**: 在该文件所在目录下创建 `temp_versions` 文件夹（若不存在）。
      - **步骤 B (移存)**: 将当前旧文件重命名为 `{原文件名}_backup_{timestamp}.docx` 并移动至 `temp_versions`。
      - **步骤 C (生成)**: 用新数据生成一个全新的文件，命名为 `{原文件名}.docx` (即恢复标准命名)。
      - **步骤 D (标记)**: 将该文件记录的状态更新为 `OUTDATED_MANUAL_MERGE_REQUIRED`。
      - **UI 提示**: 文件列表该项显示醒目的橙色警告：“数据已更新，需合并手动修改”。

### 4. 用户合并操作工作流
当用户看到“需合并手动修改”标记时：
1.  用户打开当前的新文件（含最新数据，无红色字）。
2.  用户进入 `temp_versions` 文件夹，打开备份文件（含红色字的手动修改）。
3.  用户将备份文件中的**红色字体内容**复制或参考填入新文件。
4.  用户保存新文件，并通过系统界面点击 **[✅ 确认更新并完成合并]** 按钮。
5.  **系统动作**:
    - 将 `sync_status` 更新为 `USER_MODIFIED` (因为合并后它又包含人工内容了)。
    -  `temp_versions` 中的旧备份文件，只保留最近 替换的 2 个版本。

## Technical Requirements
- **文件操作**: 严格遵循分布式文件代理架构，所有移动、重命名、新建操作必须通过后端 API (`POST /api/files/move`, `POST /api/files/generate`) 在客户端本地执行。
- **UI 交互**: 
  - 文件列表增加状态列。
  - 提供“确认合并”的操作按钮。

## Goal
实现一个既保证数据最新，又不丢失用户个性化修改的稳健流程。宁可让用户多一步“确认合并”的操作，也绝不静默覆盖用户的心血。