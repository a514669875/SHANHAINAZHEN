# Role
你是本项目的核心前端开发专家。我们需要在“采购项目详情页”的下半部分（文件管理区域）完善“打印”功能。

# Context
- **项目架构**: Vue 3 + TypeScript + Element Plus + FastAPI。
- **文件存储**: 分布式架构。文件不在服务器集中存储，而是存储在经办人本地。前端通过后端代理接口获取文件流 (`GET /api/files/proxy?file_id=xxx`)。
- **当前页面**: `ProjectPurchaseDetail.vue`。下半部分有两个 Tab 页签：“流程文件”和“归档文件”，展示文件列表（包含文件名、类型、上传时间等）。
- **需求来源**: 基于上传的 `打印功能.docx` 文档。

# Goal
实现一个智能打印功能，允许用户勾选多个文件，合并预览并调用浏览器打印。

# Detailed Requirements (基于打印功能.docx)

## 1. 触发入口
- 在“流程文件”和“归档文件”列表的顶部工具栏的 **[🖨️ 一键打印勾选]** 按钮。
- 只有当用户勾选了至少一个文件复选框时，该按钮才可用（Enabled），否则禁用。

## 2. 打印模式逻辑
点击按钮后，弹出一个模态框 `PrintPreviewModal`：

### 原文打印 (适用于流程文件/归档扫描件)
- **逻辑**: 
  1. 获取用户勾选的所有文件 ID 列表。
  2. 按列表顺序，依次调用后端代理接口获取文件二进制流 (Blob)。
  3. **关键处理**: 
     - 如果是 PDF: 直接使用浏览器的原生 PDF 查看器或 `pdf.js` 进行连续渲染。
     - 如果是 Word (.doc/.docx): 由于浏览器无法直接打印 Word，需调用后端接口 `/api/files/convert-to-pdf` (假设存在) 将其转换为 PDF 流后再渲染；或者提示用户“Word 文件需下载后打印”，若 docx 中已包含转换服务则自动转换。
  4. **预览界面**: 在一个长页面中逐个展示所有文件的预览。
  5. **操作**: 底部提供 [打印] 按钮，调用 `window.print()`。

## 3. 技术实现约束
- **文件获取**: 必须使用现有的文件代理机制。不要尝试直接访问本地路径。
  ```typescript
  // 示例：获取文件流（不强制要求使用该示例，一切以系统原本设计规则为准）
  const downloadFile = async (fileId: string) => {
    return await api.get(`/files/proxy/${fileId}`, { responseType: 'blob' });
  };

  