# 🛑 全局架构重构指令：消除系统所有数据滞后，实现全链路实时同步

> **已定稿**：详见 PRD 8.23、development 2.4。

## 1. 核心问题诊断
当前系统存在严重的**数据最终一致性滞后**问题。
- **现象**：用户在执行操作（如删除项目、更新模板、修改配置）后，前端界面不会立即更新，必须手动刷新页面或切换菜单才能看到最新状态。
- **根源**：系统目前完全依赖“请求 - 响应”模式。前端只在初始化或手动触发时拉取数据，而**存储节点执行完任务后，没有主动通知后端，后端也没有主动推送变化给前端**。
- **目标**：将系统从“被动轮询”升级为**“事件驱动（Event-Driven）”架构**，实现**毫秒级实时同步**。任何状态变更（数据库、文件系统、配置）必须在 1 秒内自动反映到所有相关客户端。

## 2. 强制性架构规范 (必须严格执行)

请按照以下三个层面的规范，重构现有代码逻辑：

### A. 通信层：引入 WebSocket 全双工通道
- **禁止**：在前端使用 `setInterval` 轮询来获取状态更新。
- **强制**：
  1. 建立 **后端 <-> 前端** 的 WebSocket 长连接。
  2. 建立 **存储节点 (Storage Node) <-> 后端** 的 WebSocket 长连接，存储节点执行完写/删后主动上报事件。
  3. 定义统一的 **事件协议 (Event Protocol)**，所有状态变更必须通过此协议广播。

### B. 数据流层：实现“动作 -> 事件 -> 广播 -> 更新”闭环
对于**任何**改变系统状态的操作（增删改查、文件操作、配置变更），必须遵循以下流程：
1. **触发**：用户发起动作 或 本地代理检测到变化。
2. **执行**：后端或本地代理执行具体逻辑（如删除文件、写入数据库）。
3. **发射 (Emit)**：执行成功后，**立即**生成一个标准事件对象（包含 `type`, `payload`, `timestamp`）。
4. **广播 (Broadcast)**：后端通过 WebSocket 将该事件推送给所有订阅了该资源的前端客户端。
5. **消费 (Consume)**：前端监听事件，**直接操作本地 State (Redux/Vuex/Context)** 更新 UI，**严禁**触发整页刷新或重新请求列表接口。

### C. 存储节点 (Storage Node Event Reporter)
- **职责**：负责实际文件操作 + 上报事件。
- **通信**：HTTP 用于指令下发，WebSocket 用于事件推送。
- **改造**：每次文件操作（创建、修改、删除、移动）完成后，必须向后端发送事件。

## 3. 具体实施任务清单

请依次完成以下重构任务：

### 任务 1：定义统一事件协议
创建一个 `events_schema.py` (后端) 和 `event_types.ts` (前端)，定义所有可能的系统事件，例如：
- `PROJECT_DELETED`: { id, path }
- `TEMPLATE_UPDATED`: { project_id, file_name, hash }
- `CONFIG_CHANGED`: { user_id, key, value }
- `SYNC_COMPLETED`: { task_id, status }

### 任务 2：后端 WebSocket 服务搭建
- 集成 WebSocket 库 (如 Python `FastAPI/WebSockets` 或 `Flask-SocketIO`)。
- 实现连接管理：记录 `user_id` 与 `websocket_connection` 的映射。
- 实现广播方法：`broadcast_to_user(user_id, event)` 和 `broadcast_to_all(event)`。

### 任务 3：存储节点 (Storage Node) 接入
- 存储节点在每次文件操作成功后，通过 WebSocket 调用 `emit_event()` 上报后端。
- 确保网络断开时有本地队列缓存，网络恢复后重传（保证可靠性）。

### 任务 4：前端状态管理重构
- 移除所有用于“同步状态”的 `useEffect` 轮询定时器。
- 封装 `useRealtimeSync()` Hook：
  - 自动连接 WebSocket。
  - 监听各类事件。
  - 根据事件类型自动更新对应的 Store/State。
- 验证场景：删除项目、新增模板、修改配置，确保 UI **无刷新即时变动**。

### 任务 5：乐观更新规则
- **✅ 乐观更新**：新增/编辑文件内容、重命名文件/项目。
- **❌ 不做乐观更新**：删除操作——需二次确认 + 后端成功后再刷新。

## 4. 验收标准
重构完成后，系统必须满足：
1. **零延迟感**：用户在 A 窗口操作，B 窗口（或同一窗口列表）在 500ms 内自动更新，无需任何手动干预。
2. **无轮询**：代码中不得出现用于数据同步的 `setInterval` 或 `setTimeout` 轮询逻辑。
3. **全场景覆盖**：不仅限于“删除项目”，包括文件内容变更、配置修改、任务状态流转等所有场景。

## 5. 开始行动
请先分析当前代码库中涉及数据同步的关键文件（前端 List 组件、后端 Controller、本地代理 Executor），然后输出重构计划，并开始编写核心代码（事件协议定义 + WebSocket 服务端）。